name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: Build, verify, score
    runs-on: warp-ubuntu-2404-x64-32x
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      # Change the dependency cache only when this repository's import surface
      # changes. Ordinary proof edits can reuse the same package build, while a
      # new import gets a fresh key that can save the expanded closure.
      - name: Fingerprint Lean imports
        id: lean-imports
        run: |
          import_hash="$({ git grep --no-color '^import ' -- '*.lean' || true; } | sort | sha256sum | cut -d' ' -f1)"
          echo "hash=$import_hash" >> "$GITHUB_OUTPUT"

      # Cache this repository separately from its pinned dependencies. The
      # restore prefix gives Lake a safe incremental base after source edits.
      - name: Cache compiled project build
        uses: actions/cache@v5
        with:
          path: .lake/build
          key: project-lake-build-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('lean-toolchain', 'lake-manifest.json') }}-${{ hashFiles('Challenge/**/*.lean', 'Challenge.lean', 'Checks.lean', 'Main.lean', 'lakefile.toml') }}
          restore-keys: |
            project-lake-build-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('lean-toolchain', 'lake-manifest.json') }}-

      # Preserve complete package workspaces, including Git metadata. Caching
      # only their build directories makes Lake regard the restored packages
      # as invalid checkouts and delete the artifacts before building.
      - name: Cache pinned packages
        id: package-cache
        uses: actions/cache@v5
        with:
          path: .lake/packages
          key: lake-packages-v2-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('lean-toolchain', 'lake-manifest.json') }}-${{ steps.lean-imports.outputs.hash }}
          restore-keys: |
            lake-packages-v2-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('lean-toolchain', 'lake-manifest.json') }}-

      - name: Build (fetch Mathlib cache on cold start + lake build)
        uses: leanprover/lean-action@v1
        with:
          # `cache-hit` is empty only for a total miss; it is `false` for a
          # restore-prefix hit, whose complete workspace already has Mathlib.
          use-mathlib-cache: ${{ steps.package-cache.outputs.cache-hit == '' }}
          build: "true"
          test: "false"
          lint: "false"
          use-github-cache: "false"

      # A proof is only a proof if its axiom footprint is the standard one.
      # Checks.lean pins it with `#guard_msgs`, so a `sorry` (`sorryAx`) or a
      # `native_decide` (`Lean.ofReduceBool`) fails the job. The textual scan
      # catches an unfinished proof in a file Checks.lean does not reach.
      - name: Verify soundness (axiom footprint, no unfinished proofs)
        run: |
          if grep -rn --include='*.lean' -w 'sorry' Challenge Challenge.lean Main.lean; then
            echo "::error::found an unfinished proof"
            exit 1
          fi
          lake env lean Checks.lean

      # Candidate PRs follow a fixed module/declaration convention. This gate
      # checks the theorem type, bytecode/hex identity, transitive axiom set,
      # and executable vectors without trusting contributor-written checks.
      # Its negative control proves that a type-correct fake axiom is rejected.
      - name: Verify SHA-256 candidate submissions
        run: |
          scripts/check-sha256-submissions.sh --self-test
          scripts/check-sha256-submissions.sh

      # Keep the statement auditable in isolation. Proof support and the
      # bundled implementation may depend on the spec, never the reverse.
      - name: Verify SHA specification boundary
        run: |
          imports="$(grep '^import ' Challenge/Sha256/Spec.lean)"
          if [ "$imports" != 'import EvmSemantics.EVM.BigStep' ]; then
            echo "::error::Spec.lean must depend only on the pinned EVM semantics"
            printf '%s\n' "$imports"
            exit 1
          fi

      - name: Verify MODEXP specification boundary
        run: |
          imports="$(grep '^import ' Challenge/Modexp/Spec.lean)"
          if [ "$imports" != 'import EvmSemantics.EVM.BigStep' ]; then
            echo "::error::MODEXP Spec.lean must depend only on the pinned EVM semantics"
            printf '%s\n' "$imports"
            exit 1
          fi

      # `reference.hex` is the frozen direct-bytecode artifact. The compiler is
      # only its reproducible generator; its bytecode proof uses no compiler
      # soundness theorem.
      - name: Verify frozen reference bytecode
        run: |
          compiled="$(lake exe yulc Challenge/Sha256/Reference/reference.yul)"
          frozen="$(tr -d '[:space:]' < Challenge/Sha256/Reference/reference.hex)"
          literal="$(lake exe sha256challenge --print-reference-hex)"
          if [ "$compiled" != "$frozen" ] || [ "$literal" != "$frozen" ]; then
            echo "::error::reference.hex is stale"
            exit 1
          fi

      - name: Verify frozen MODEXP reference bytecode
        run: |
          compiled="$(lake exe yulc Challenge/Modexp/Reference/reference.yul)"
          frozen="$(tr -d '[:space:]' < Challenge/Modexp/Reference/reference.hex)"
          literal="$(lake exe modexpchallenge --print-reference-hex)"
          if [ "$compiled" != "$frozen" ] || [ "$literal" != "$frozen" ]; then
            echo "::error::MODEXP reference.hex is stale"
            exit 1
          fi

      - name: Tier 1 — MODEXP reference vectors
        run: lake exe modexpchallenge

      # Score the reference and every submission, validate optional proved
      # input-size schedules, and keep the checked gas tables in the SHA README
      # synchronized with the exact submitted artifacts.
      - name: Verify and report SHA-256 gas
        run: |
          scripts/report-sha256-gas.sh --self-test
          scripts/check-sha256-gas-report.sh

      # The scorer must be able to reject: a candidate that returns 32 zero
      # bytes has to fail every vector. A green Tier-1 gate that cannot fail
      # is worse than no gate.
      - name: Tier 1 — negative control
        run: |
          printf '60206000f3' > /tmp/zeros.hex
          if lake exe sha256challenge --hex=/tmp/zeros.hex; then
            echo "::error::the scorer accepted a candidate returning 32 zero bytes"
            exit 1
          fi

      # RIPEMD-160 uses the same minimal-spec/frozen-artifact discipline as
      # SHA-256.  Keep the independently checked source, literal bytecode, and
      # executable artifact synchronized.
      - name: Verify RIPEMD-160 specification boundary and frozen bytecode
        run: |
          imports="$(grep '^import ' Challenge/Ripemd160/Spec.lean)"
          if [ "$imports" != 'import EvmSemantics.EVM.BigStep' ]; then
            echo "::error::RIPEMD-160 Spec.lean must depend only on the pinned EVM semantics"
            printf '%s\n' "$imports"
            exit 1
          fi
          compiled="$(lake exe yulc Challenge/Ripemd160/Reference/reference.yul)"
          frozen="$(tr -d '[:space:]' < Challenge/Ripemd160/Reference/reference.hex)"
          literal="$(lake exe ripemd160challenge --print-reference-hex)"
          if [ "$compiled" != "$frozen" ] || [ "$literal" != "$frozen" ]; then
            echo "::error::RIPEMD-160 reference.hex is stale"
            exit 1
          fi

      - name: Verify RIPEMD-160 reference and scorer negative control
        run: |
          scripts/check-ripemd160-submissions.sh --self-test
          scripts/check-ripemd160-submissions.sh
          scripts/report-ripemd160-gas.sh --self-test
          scripts/check-ripemd160-gas-report.sh
          lake exe ripemd160challenge --hex=Challenge/Ripemd160/Reference/reference.hex
          printf '60206000f3' > /tmp/ripemd-zeros.hex
          if lake exe ripemd160challenge --hex=/tmp/ripemd-zeros.hex; then
            echo "::error::the RIPEMD-160 scorer accepted a candidate returning 32 zero bytes"
            exit 1
          fi
