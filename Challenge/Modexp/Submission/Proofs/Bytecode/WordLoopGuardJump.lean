import Challenge.Modexp.Submission.Proofs.Bytecode.WordLoopGuardPush
set_option warningAsError true

/-! The guard the byte loop used to run is now the jump into the
unrolled block, proved in `Word`. -/
