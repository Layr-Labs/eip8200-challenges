# Montgomery n=8 eager-T8 cached-MAC — fresh-base restack

Base: `ae7bee26443011c5ce256e58bd913185fb933403` (authoritative score 851956 at handoff).
Bytecode: 6097 bytes, SHA256 `ce0325cee4eef2ab9efa8c3b28a88cd74d0a44f698697209bc45d4179f04c4fc`.
Explicitly authorized restack cutoff: 2026-09-08T13:54:00+07:00.
Parent owns normal hosted submission; this worker does not submit.

## Integration and preservation

Restacked the complete integrated source from cdfc293ab9896e2aeb867ba23731f1a68237232b.
Preserved the exact fresh-base bytes except the five-byte Montgomery entry trampoline;
the appended mechanism has unchanged PCs and eight fewer preceding instruction ordinals.
Regenerated Bytes, Artifact, and MonproN8Paths; corrected appended ordinals in Monpro,
MonproN8Mac, and MonproN8Rows. Preserved the fresh public-window changes and inherited
fresh-base legacy-path ordinals. No edits to the primary dirty PR767 checkout or old source.

Eager T[8] stores preserve all incumbent intermediate memories. MonproCore extracts the
existing arithmetic/state foundation. MonproN8Memory/Mac/Rows provide exact memory,
arithmetic, branch, terminal-row, and eight-row composition source. P7 binds the real
entry trampoline. Monpro.run_mpEntry includes the relocated prologue and both selector
outcomes; gasSteps_monproToCsub chooses n=8 rows or incumbent fallback. The existing
Exp.Subroutines.monpro witness reaches this integration with unchanged public contracts.

## Executed evidence (not kernel proof)

Run Python scripts in this directory:
- test_current.py: 78 whole Montgomery calls against exact fresh-base opcodes; n=8 saves
  3852 gas per call, other tested widths cost 54 more; peak stack 1022; mutation detected.
  Independently reconstructs both full byte-binding surfaces.
- test_mac_contracts.py: 240 cases across all 15 contracts, aliases/unaligned addresses.
- test_memory_order.py: 64 arbitrary rows, 19 matching store snapshots, 487 gas saved/row.
- test_integration.py: 1553 located claims, 51-module Exp import closure, Rows reachable,
  unchanged public contracts and no proof holes in changed integration modules.
- test_restack.py: complete Solution closure, active located/PC/jump claims, acyclic
  imports, byte chunks including final partial chunk, instruction count, public-byte
  preservation, no conflict markers or proof holes.

No compiler, Comparator, scorer, hosted validation, or review was run. Complete integrated
source is uncompiled; these tests do not establish elaboration or an official gas score.
The identity.json `eligible: false` is conservative legacy metadata, not an ancestry test
or a requirement for full local Lean validation. Parent must run normal upload preflight.

## Generator caveat

build_ordered.py --emit regenerates byte bindings and MonproN8Paths only; it does not
regenerate hand-integrated Core/Rows/Monpro. generate_traces.py remains STALE: never run it
over the hand-corrected Mac module. Its ordinal assumptions also predate this restack.
Replay the complete parent artifact against the exact base with `git apply --index`.
