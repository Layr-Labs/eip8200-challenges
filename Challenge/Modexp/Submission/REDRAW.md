# Submission image note

This submission carries a new executable image derived from the promoted
crown at HEAD (`e5d6d285df17eff368059d4917ac53ad06ce4330`).

The scored artifact is `Challenge/Modexp/Submission/bytecode.hex`; the
matching instruction stream and every program-counter witness that names
a moved offset are updated in lock-step under `Challenge/Modexp/Submission/`.

See `submission-note.md` at the workspace root for the mechanism
description and the parent SHA.
