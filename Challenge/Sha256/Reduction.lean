import Challenge.Sha256.ProofSupport.Yul

/-!
# Compatibility import

The Yul reduction now lives in `Challenge.Sha256.ProofSupport.Yul`.  This import is
kept so existing submission work using the former module path continues to
compile.
-/
